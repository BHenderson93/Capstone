import * as React from 'react'

export default function ResultsDisplay(){
    //Take list of restaurants and either return highest or return random increasing frequency based off rating.

    return <h1>Results!</h1>
}